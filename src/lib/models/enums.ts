export enum ENUM_CW_STATES {
  noWarnings,
  questionable,
  explicit,
}

export enum ENUM_AI_WARNING_TYPE {
  none,
  verified,
  suspected,
}

export enum ENUM_IMAGE_EMBED_SOURCE_TYPE {
  yt,
  nn,
  bb,
  vdb,
}
